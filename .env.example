# Copie este arquivo para ".env.local" (desenvolvimento) e configure no
# ambiente de build de produção antes do "npm run build".

# ID do container do Google Tag Manager, ex.: GTM-XXXXXXX
# [VALIDAR COM O CLIENTE]
NEXT_PUBLIC_GTM_ID=
